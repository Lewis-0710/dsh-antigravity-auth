/** Independent Video row; the Gate V tool is intentionally not mounted yet. */

export const name = 'antigravity-video'

export function apply(): void {
  // This bootstrap row owns no tool and performs no network work.
}
