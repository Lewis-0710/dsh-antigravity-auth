/** Independent Search row; the Gate S provider is intentionally not mounted yet. */

export const name = 'antigravity-search'

export function apply(): void {
  // This bootstrap row owns no provider and performs no network work.
}
