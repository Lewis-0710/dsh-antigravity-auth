/** Independent Image row; the Gate I tools are intentionally not mounted yet. */

export const name = 'antigravity-image'

export function apply(): void {
  // This bootstrap row owns no tools and performs no network work.
}
